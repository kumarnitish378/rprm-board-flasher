This folder is the firmware library. Any .hex or .bin file here appears in the
program's "From library" list, named by manifest.json if one is present.

SAMPLE-random-test-pattern.hex is NOT firmware. It is 4 KB of generated random
bytes, used to exercise the tool's file handling. Do not flash it to a board you
care about.
